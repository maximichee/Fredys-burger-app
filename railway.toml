[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @fredys/api build"

[deploy]
startCommand = "pnpm --filter @fredys/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
