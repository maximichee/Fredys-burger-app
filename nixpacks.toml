[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@9.15.0", "pnpm install --ignore-scripts"]

[phases.build]
cmds = ["pnpm --filter @fredys/api build"]

[start]
cmd = "pnpm --filter @fredys/api start"
